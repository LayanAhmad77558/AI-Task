# AI-Task
Task2
# 🎨 Color Recognition using OpenCV

This project detects red and blue colors in real-time using a webcam, with the help of OpenCV and NumPy libraries in Python.

---

## 📷 How it works

- Opens your webcam and continuously reads the video feed.
- Converts the frame to HSV color space.
- Detects red and blue colors using HSV thresholds.
- Displays messages:
  - "Red Detected" when red objects appear.
  - "Blue Detected" when blue objects appear.
- Press q to quit the program.

---

## 🚀 Requirements

- Python 3.x
- OpenCV
- NumPy

### 📦 Install dependencies:

```bash
pip install opencv-python numpy
